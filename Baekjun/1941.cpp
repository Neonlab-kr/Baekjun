//#include <iostream>
//#include <string>
//#include <regex>
//using namespace std;
//
//int main() {
//	string input = "";
//	cin >> input;
//	input = regex_replace(input, regex("c="), "a");
//	input = regex_replace(input, regex("c-"), "a");
//	input = regex_replace(input, regex("dz="), "a");
//	input = regex_replace(input, regex("d-"), "a");
//	input = regex_replace(input, regex("lj"), "a");
//	input = regex_replace(input, regex("nj"), "a");
//	input = regex_replace(input, regex("s="), "a");
//	input = regex_replace(input, regex("z="), "a");
//	cout << input.length();
//}